apiVersion: v1
kind: Pod
metadata:
  name: clickhouse
  namespace: ${NAMESPACE_CLICKHOUSE}
  labels:
    app: clickhouse
spec:
  imagePullSecrets:
    - name: dockerhub-pull
  containers:
    - name: clickhouse
      image: commodore95/quant_data_platform_repository:clickhouse-dev
      imagePullPolicy: IfNotPresent
      env:
        - name: TZ
          value: "Europe/Zurich"
        - name: CLICKHOUSE_USER
          value: "spark"
        - name: CLICKHOUSE_PASSWORD
          value: "sparkpass"
      ports:
        - name: http
          containerPort: 8123
        - name: tcp
          containerPort: 9000
        - name: interserver
          containerPort: 9009
      readinessProbe:
        httpGet:
          path: /ping
          port: 8123
        initialDelaySeconds: 5
        periodSeconds: 5
      livenessProbe:
        httpGet:
          path: /ping
          port: 8123
        initialDelaySeconds: 10
        periodSeconds: 10
  restartPolicy: Always
